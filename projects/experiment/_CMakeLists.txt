
if(OVR_BUILD_OPTIX7)
  add_subdirectory(mini_optix7 EXCLUDE_FROM_ALL)
endif()

if(OVR_BUILD_OSPRAY)
  add_subdirectory(mini_ospray EXCLUDE_FROM_ALL)
endif()

# if(OVR_BUILD_TORCH)
#   add_subdirectory(mini_pytorch  EXCLUDE_FROM_ALL)
#   add_subdirectory(mini_tensorrt EXCLUDE_FROM_ALL)
# endif()
